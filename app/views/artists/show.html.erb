<p id="notice"><%= notice %></p>

<p>
  <strong>Songs by <%= @artist.name %></strong>
</p>
<p>
  <strong>Genre: <%= @artist.genre.name %></strong>
</p>

<ul>
  <% @artist.songs.order('name').each do |song| %>
  <li><%= song.name %></li>
<% end %>
</ul>

<%= link_to 'Edit', edit_artist_path(@artist) %> |
<%= link_to 'Back', artists_path %>
